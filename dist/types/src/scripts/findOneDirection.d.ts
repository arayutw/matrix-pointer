import { Id, Matrix, Position } from "./class";
export declare function findOneDirectionY(matrix: Matrix, nx: number, sy: number | null, next: boolean): Position | null;
export declare function findOneDirectionX(row: Id[], sx: number | null, ny: number, next: boolean): Position | null;
